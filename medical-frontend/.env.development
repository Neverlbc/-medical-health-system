   VITE_APP_BASE_API=http://localhost:8080
   ```
2. 安装依赖并启动：
   ```bash
   cd medical-frontend
   npm install
   npm run dev
   ```
   默认在 `http://localhost:5173` 访问，`vite.config.ts` 已配置 `/api` 代理到后端。

## 后续建议
- 将各页面中的示例静态数据替换为调用后端接口的逻辑，可从 `src/api` 继续扩展模块。
- 根据角色与业务补充路由/菜单、组件拆分、鉴权策略及国际化等。
- 引入实际图表/表格数据时，可结合后端 `doc.html` 中接口定义完善参数与数据结构。